default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  pool: 5

development:
  <<: *default
  database: fresh-auth-backend_development
  username: roshan
#
# production:
#   <<: *default
#   database: fresh-auth-backend_production
#   username: fresh-auth-backend
#   password: <%= ENV['FRESH-AUTH-BACKEND_DATABASE_PASSWORD'] %>
